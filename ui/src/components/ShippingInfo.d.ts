import { Market } from '@/types';
interface ShippingInfoProps {
    selectedMarket: Market;
}
export declare const ShippingInfo: ({ selectedMarket }: ShippingInfoProps) => import("react/jsx-runtime").JSX.Element;
export {};
