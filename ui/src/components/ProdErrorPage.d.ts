import type { ReactNode } from "react";
interface Props {
    text: ReactNode;
    canRefresh: boolean;
}
export declare const ProdErrorPage: ({ text, canRefresh }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
