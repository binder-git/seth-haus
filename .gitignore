*.env
.env.*
# Local Netlify folder
.netlify
# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.python-version 
# If you have other venv names, add them too, e.g., venv/, env/

# Node / Vite / React
node_modules/
dist/ # If your Vite build output 'dist' is within ui/ and ui/dist is your publish dir, this is okay.
      # If you have a top-level dist/ that's different, consider that.
.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# IDE / Editor specific
.vscode/
.idea/

# Netlify specific (usually not needed if config is in netlify.toml)
# .netlify/ 
# Usually, you only want to ignore .netlify/state.json or similar local dev state if anything.
# For functions, the source is in netlify/functions, which IS committed.